#ifndef TRHANDLER_H
#define TRHANDLER_H

#include <QThread>
#include <xing/xaquery.h>
class TrQueryHandler : public QThread
{
    Q_OBJECT;
public:
    TrQueryHandler();
    ~TrQueryHandler();
protected:
    long changeStringToLong(char * strLong, int length);
    void run();

private:

public slots:
    virtual void responseReceived(LPRECV_PACKET packet)=0;
};

#endif // TRHANDLER_H
